# 引き継ぎ 2026-07-24

## 現在地

コードはまだ1行も書かれていない。あるのは次の3つ。

- `nyukin-daicho.html` … 単一HTMLのモック。実データ54件入り。ブラウザで開けば動く。
  本日の作業 / 懸案 / 台帳 / 入金表 の4画面。消し込み、通帳照合、名寄せ、
  累計差額、履歴の表示までを含む。データはメモリ上のみで保存されない
- `支払予定明細_2026-07時点.csv` … 2,592行。ADR-002 より前の生成物のため
  ボーナス加算を含んでいる。使うなら列を分離すること
- `docs/` … 仕様、設計判断、移行手順、未決定事項

設計の合意は取れている。運用ルールが3件(open-questions B-1〜B-3)未確定。

## 次の一手

**モックから作り直さない。** モックは1,600行の単一HTMLで、置換スクリプトでしか
編集していない。動作の定義としては正確だが、コードの構造としては参考にしない。

最初にやることは、`docs/spec.md` のデータモデルをスキーマに落とすこと。順序は
契約 → 支払予定 → 入金 → 充当 → 事象 → 名寄せ辞書。

事象テーブルと記録者の欄を最初に作ること。**後から遡って埋められない唯一のもの**であり、
このシステムの主目的(引き継ぎ可能にすること)がここに乗っている。

技術選定はしていない。PWA でスマホからも使う方針だけが決まっている。

## 罠

- **モックのHTMLを編集して進めようとしない。** 1文字直すのに置換スクリプトが要る状態になる
- **ボーナス加算を支払予定に入れない。** CSVには入っている。ADR-002 を読むこと
- **入金レコードに請求IDを1本だけ持たせない。** まとめ払いで即詰みする。ADR-003
- **画面に「消込」「充当」「債権」と書かない。** CLAUDE.md の規約
- **決まっていない運用ルールを画面で仮決めしない。** 画面の形が運用を決めてしまう
- 実データに氏名と携帯番号が含まれる。テストデータへの流用と、リポジトリへの混入に注意

## 未検証

- モックのブラウザ動作は手元で目視確認していない。JSの構文と、
  データの整合(列数、ID重複、金額の検算)のみ機械的に確認済み
- 印刷レイアウトは未確認
- スマホ実機での確認は、担当者に触ってもらう段階でのみ実施。iOSでの表示崩れを1度修正した
- 移行の検算スクリプトは会話の中で1度動かしただけで、再実行可能な形にはなっていない
